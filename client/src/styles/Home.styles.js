import styled from 'styled-components';

export default styled.div`
  h1 {
    text-align: center;
    margin: 3rem 0;
  }

  .recipe-list {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    column-gap: 2rem;
    row-gap: 2rem;
    justify-content: space-around;
  }
`;
