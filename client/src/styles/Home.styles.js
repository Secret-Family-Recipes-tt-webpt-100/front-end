import styled from 'styled-components';

export default styled.div`
  .recipe-list {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    row-gap: 2rem;

    margin: 0 auto;
    align-items: center;
  }
`;
